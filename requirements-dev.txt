# Development only. The product — scripts/forwarding.py, scripts/mcp_server.py, api/ — is
# stdlib and needs none of this; a judge runs it with a bare python3.
pytest>=9.1.1
pytest-cov>=7.1
hypothesis>=6.168.0
ruff>=0.16.7
mypy>=2.3.1
pip-audit>=2.10.1
