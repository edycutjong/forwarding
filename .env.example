# Forwarding Address needs NO secrets to run.
# Every endpoint on the judged path is CoinMarketCap's keyless /public-api surface.
#
# Optional escape hatch — only if the anonymous tier has throttled your IP, never for the demo:
# CMC_API_KEY=
